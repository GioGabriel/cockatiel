name: vocal_coach_app
description: Flutter client scaffold for Vocal Coach
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  cloud_firestore: ^6.1.3
  firebase_auth: ^6.2.0
  firebase_core: ^4.5.0
  flutter:
    sdk: flutter
  flutter_local_notifications: ^19.4.2
  google_fonts: ^6.3.0
  http: ^1.2.2
  lottie: ^3.1.0
  record: ^6.1.1
  shared_preferences: ^2.2.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
