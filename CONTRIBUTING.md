# Contributing

This repository uses a contracts-first and modular architecture approach.

Basic workflow:
1. Update contracts in `contracts/` first.
2. Implement backend/mobile changes against versioned contracts.
3. Keep module boundaries clean and avoid cross-feature coupling.
